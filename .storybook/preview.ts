import type { Preview } from '@storybook/react';
import '../src/tokens/theme.css';
import '../src/tokens/admin-theme.css';

const preview: Preview = {
  parameters: {
    layout: 'centered',
  },
};

export default preview;
